server:
  port: ${MONITORING_SERVER_PORT}

turbine:
  stream:
    port: ${MONITORING_TURBINE_PORT}

spring:
  rabbitmq:
    password: ${RABBITMQ_PASSWORD}
    username: ${RABBITMQ_USERNAME}
    host: ${RABBITMQ_HOST}

logging:
  level:
    org.springframework: INFO